<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Patients</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
   <%- include('partials/navbar') %> 
  <div class="container py-5">
    <h2 class="mb-4">All Registered Patients</h2>

     <div class="list-group">
     <!-- <% patients.forEach(p => { %>
     <a href="/<%= p.name %>" class="list-group-item list-group-item-action">
     <%= p.name %>
     </a>
     <form method="POST" action="/delete/<%= p.name %>" onsubmit="return confirm('Are you sure you want to delete <%= p.name %>?');">
            <button type="submit" class="btn btn-sm btn-danger px-3 py-1">Delete</button>
     </form>
     <% }) %> -->
     <% patients.forEach(p => { %>
    <div class="list-group-item d-flex justify-content-between align-items-center">
      <a href="/<%= p.name %>" class="text-decoration-none text-dark fw-semibold">
        <%= p.name %>
      </a>
      <form method="POST" action="/delete/<%= p.name %>" 
            onsubmit="return confirm('Are you sure you want to delete <%= p.name %>?');" 
            class="m-0">
        <button type="submit" class="btn btn-sm btn-outline-danger px-3 py-1">
         <i class="fa-solid fa-trash"></i> Delete
        </button>
      </form>
    </div>
    <% }) %>
    </div> 

   

    <a href="/" class="btn btn-secondary mt-4">Back to Dashboard</a>
  </div>
</body>
</html>
